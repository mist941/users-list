import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  pageWrapper: {
    marginTop: 10,
  },
  emptyUsersNotification: {
    textAlign: "center",
    fontSize: 25,
    marginTop: 15,
    color: "#696969",
  }
});
